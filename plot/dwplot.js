var dwPlot = (function (plot) {
    var that = {};
    
    // draw axis
    
    // store background into buffer
    
    that.clear = function () {
        context.putImageData(bgBuffer, 0, 0);
    };
})();